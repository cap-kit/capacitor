{
  "name": "capacitor",
  "private": true,
  "packageManager": "pnpm@11.23.0",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=11.0.0"
  },
  "workspaces": [
    "android",
    "ios",
    "cli",
    "core"
  ],
  "scripts": {
    "ci:publish:nightly": "lerna version prerelease --conventional-commits --conventional-prerelease --preid nightly-$(date +\"%Y%m%dT%H%M%S\") --force-publish --no-changelog --no-git-tag-version --no-push --yes && lerna exec -- pnpm publish --tag nightly --provenance",
    "ci:publish:alpha": "lerna version prerelease --conventional-commits --conventional-prerelease --preid alpha --force-publish --yes && lerna exec -- pnpm publish --tag next --provenance",
    "ci:publish:beta": "lerna version prerelease --conventional-commits --conventional-prerelease --preid beta --force-publish --yes && lerna exec -- pnpm publish --tag next --provenance",
    "ci:publish:rc": "lerna version prerelease --conventional-commits --conventional-prerelease --preid rc --force-publish --yes && lerna exec -- pnpm publish --tag next --provenance",
    "ci:publish:latest": "lerna version --conventional-commits --force-publish --yes && lerna exec -- pnpm publish --tag latest --provenance",
    "ci:publish:latest-from-pre": "lerna version --conventional-commits --conventional-graduate --force-publish --yes && lerna exec -- pnpm publish --tag latest --provenance",
    "ci:publish:dev": "lerna version prerelease --conventional-commits --conventional-prerelease --force-publish --preid dev-$(date +\"%Y%m%dT%H%M%S\") --no-changelog --no-git-tag-version --no-push --yes && lerna exec -- pnpm publish --tag dev --provenance",
    "clean": "pnpm -r run clean",
    "build": "pnpm -r run build",
    "build:nativebridge": "pnpm --filter @capacitor/core run build:nativebridge",
    "download:official": "node scripts/download-official.mjs",
    "download:pr": "node scripts/download-pr-patch.mjs",
    "create-patch": "node scripts/create-patch.mjs",
    "combine-patches": "node scripts/combine-patches.mjs",
    "sync-peer-dependencies": "node scripts/sync-peer-dependencies.mjs",
    "lint": "eslint . && prettier --plugin=prettier-plugin-java --check \"**/*.{css,html,java,js,mjs,ts}\" && node-swiftlint lint",
    "fmt": "eslint . --fix && prettier --plugin=prettier-plugin-java --write \"**/*.{css,html,java,js,mjs,ts}\" && node-swiftlint --fix --format",
    "prettier": "prettier --plugin=prettier-plugin-java",
    "eslint": "eslint .",
    "swiftlint": "node-swiftlint",
    "version": "pnpm run sync-peer-dependencies"
  },
  "prettier": "@ionic/prettier-config",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@ionic/prettier-config": "^4.0.0",
    "@ionic/swiftlint-config": "^2.0.0",
    "@types/node": "26.2.0",
    "eslint": "^10.9.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-import": "^2.32.0",
    "globals": "^17.11.0",
    "lerna": "^10.0.1",
    "prettier": "^3.9.6",
    "prettier-plugin-java": "^2.10.3",
    "rimraf": "^6.1.3",
    "semver": "^7.8.5",
    "swiftlint": "^2.0.0",
    "tar": "^7.5.22",
    "typescript-eslint": "^8.67.0"
  }
}
